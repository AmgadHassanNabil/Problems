import { describe, expect, it } from '@jest/globals';
import { nonDivisibleSubset } from '@app/problems/non-divisible-subset'

describe('Non visible', () =>
{
    it('should be 3', () =>
    {
        // expect(nonDivisibleSubset(3, [1, 7, 2, 4])).toBe(3);
    });
    // it('should be 3', () =>
    // {
    //     expect(nonDivisibleSubset(4, [19, 10, 12, 10, 24, 25, 22])).toBe(3);
    // });
    // it('should be 11', () =>
    // {
    //     expect(nonDivisibleSubset(7, [278, 576, 496, 727, 410, 124, 338, 149, 209, 702, 282, 718, 771, 575, 436])).toBe(18);
    // });
});